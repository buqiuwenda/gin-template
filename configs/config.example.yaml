server:
  addr: ":8080"
  mode: debug # debug | release | test

data:
  driver: mysql
  source: "user:pass@tcp(127.0.0.1:3306)/gin_template?charset=utf8mb4&parseTime=True&loc=Local"

jwt:
  secret: "change-me"
  expire: 24h
